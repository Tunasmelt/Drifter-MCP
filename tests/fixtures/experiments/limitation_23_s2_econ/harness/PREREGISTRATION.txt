S2 Anthropic Economic Index (remote, public, read-only, url mode). Written before any drifter run. 2026-09-14.
Drifter commit 8861c04 (repo venv). Agent claude-code/2.1.270, http adapter.
Operator parameter_rename, seed 42. Prediction: econ_index_get_usage_by_country.country_code -> countryCode (alphabetically first eligible; required; additionalProperties false).
Prompt (verbatim): Using ONLY the econ index MCP tools (do not use any other tools), look up Japan (country code JPN) and report Japan's Anthropic Usage Index value.
Oracle: calls [econ_index_get_usage_by_country] (successful only); answer_matches = the index value from the real response used to author the fixture, fixed before the drifter run and appended below.
Corpus: 2 live observe sessions through url-mode observe. Fixture: authored from real responses for the exact recorded requests.
Runs: --repeats 4 --no-adaptive.
Predictions: baseline 4/4 valid TASK PASS. Mutated: agent sends countryCode -> inverse tier -> valid, TASK PASS.
Report as-is; no re-runs until it passes.
ORACLE FIXED BEFORE THE DRIFTER RUN: answer_matches \b1\.91\b  (real response: anthropic_usage_index 1.91, usage_rank 32 of 121).
